# CodeMonkey.Core Index

This directory contains the core business logic, models, and services shared across the solution. It includes surgical file operations (range-based read/write) via `IFileSystem` and `SecureFileSystem` to minimize token overhead and improve editing precision.